#include <bits/stdc++.h>
using namespace std;
int isSorted(int n, vector<int> arr)
{
    for (int i = 1; i < n; i++)
    {
        if (arr[i] >= arr[i - 1])
        {
        }
        else
        {
            return false;
        }
    }
    return true;
}
int main()
{
    vector<int> arr = {1, 2, 1, 3, 4};
    int n = arr.size();
    cout << isSorted(n, arr);
    return 0;
}
